# Data-pipeline dependencies: db/generate_synthetic.py, db/verify.py and ingest/.
# The agent service (agent/requirements.txt) deliberately does NOT carry these —
# they are build-time only and would bloat the Cloud Run image.
#
#   .venv/bin/pip install -r agent/requirements.txt -r ingest/requirements.txt
clickhouse-connect>=0.8,<2
google-genai>=2.10,<3
pydantic>=2.7,<3
numpy>=1.26
pyarrow>=15
