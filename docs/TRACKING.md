# Tracking
| Time (AEST) | Who | Item | Status |
|---|---|---|---|
| 13:36 | orchestrator | env ready: ADK 2.8 + mcp-clickhouse 0.6 (HTTP :8765) + CH docker + Gemini key + gcloud | done |
| 13:52 | orchestrator | tick: CH seeded (take 2479, take_event 26.5k, frame_telemetry 3.07M); 5 build agents running; local ports: API→8811 (8080/3000 taken by other apps) | in progress |
| 14:12 | data eng | db/: slateiq schema + 3.07M-row synthetic 30-day shoot, SCHEMA.md contract, verify.py 43/43 green | done |
| 13:55 | data eng | db/ done: schema + 3.07M-row synthetic shoot, verify 43/43, SCHEMA.md contract (production_id=tos2026, day12=2026-09-04) | done |
| 14:20 | frontend | web/ done: 4-screen React SPA (Ask+live MCP agent trace, Takes browser+player drawer, Production Health+DPR/TTS, About+arch SVG), mock API server, dist committed, build/typecheck green, README with API contract | done |
| 14:10 | frontend | web/ done: 4 screens, agent trace panel, dist committed (dev :5188, API :8811) | done |
| 14:32 | ingest eng | ingest/ done: 24 real day-12 takes from Tears of Steel (18 Gemini-analysed originals + 6 degraded NG variants), 232 events, 24 analyses, 8,599 telemetry rows @25Hz, 6 continuity notes → ClickHouse (tos2026, scenes 12/14A/27/33/41/56/78/102). 263s of video to Gemini (~53k tok) + 8 text-only continuity calls (~21k tok), all cached in data/cache and committed. run_all.sh idempotent, db/verify.py 43/43 green. NOTE: load.py rebuilds take_daily_agg/take_scene_agg for its keys after --replace, because the MVs feeding them never see a DELETE — anyone else deleting from `take` must do the same. .gitignore now excludes data/ except data/cache. | done |
| 14:15 | ingest | ingest/ done: 18 real clips + 6 NG variants, Gemini 3.5 flash analysis cached, telemetry 25Hz, continuity notes; verify 43/43 | done |
| 14:02 | orchestrator | tick: agent+deploy still building; launched judge-style pitch review agent (docs only) | in progress |
| 14:35 | judge/pitch | docs/JUDGE_REVIEW_1.md: scored 15/20 today (tech 4, design 4, impact 3, idea 4); top-10 fixes; rewrote DEVPOST.md + DEMO_SCRIPT.md with real DB numbers. BLOCKERS: no root README, no Cloud Run URL, hosted MCP /health = "ClickHouse connection failed", evals/last_run.md missing (README links to it). BUG: "shooting ratio" is defined as takes/circled in SCHEMA.md + prompts.py + DPR template — that is *print ratio*; real shooting ratio is material shot vs final cut. Also say "Google Cloud Agent Builder (ADK)" at least once — the rule names Agent Builder. | done |
| 14:27 | orchestrator | tick: agent+deploy still running (Grafana already on Cloud Run); launched tech-writer agent for root README + ARCHITECTURE | in progress |
| 14:34 | tech writer | root README.md (Stage-1 compliance table w/ agent/*.py file:line pointers, live-URL placeholders pending deploy/OUTPUT.md, mermaid arch, real DB numbers: 2503 takes / 26750 events / 3074957 telemetry / hero query 58ms over 3.09M rows, quickstart, evals 15/16, terminology, roadmap) + docs/ARCHITECTURE.md + docs/img/ placeholders. NOTE: my `git add` swept in agent eng's already-staged agent/evals/last_run.* + prompts/runtime/main edits — committed intact, nothing lost. deploy/ still untracked. | done |
| 14:40 | agent eng | `agent/` done. ADK 2.8 network: `slateiq_coordinator` (root) → `editor_agent` / `production_agent` / `continuity_agent` / `report_agent`, all sharing ONE `McpToolset` (StreamableHTTP → `CLICKHOUSE_MCP_URL`, bearer via `CLICKHOUSE_MCP_TOKEN`). `db/SCHEMA.md` is injected verbatim at runtime (mtime-cached, embedded fallback). Guardrails: `before_tool_callback` rejects non-SELECT/multi-statement/DDL and **rewrites** missing/oversized LIMITs to ≤200 (so the model doesn't burn a round-trip); `after_tool_callback` truncates results >24k chars. FastAPI `main.py` on **:8811** (Cloud Run uses `$PORT`): `/api/health`, `/api/chat` (SSE: session/agent/text-delta/tool_call/tool_result/final/error/done, tool_calls deduped), `/api/report/dpr`, `/api/report/editor-log`, `/api/tts` (Gemini TTS → real 24kHz wav, verified 24.3s), `/api/takes` (+ `quality_score`, `summary`, `flags[]`), `/api/take/{id}/events` (transcript + flag timeline), `web/dist` at `/` with SPA fallback (ADK's `/` redirect removed; dev UI still at `/dev-ui`), clips at `/clips`. **Evals: 16/16 questions reached MCP `run_query`, Gemini judge mean 4.88/5, min 4, 16/16 at 4+, median latency 39s** → `agent/evals/last_run.md` (+ `.json`). `docker build -f agent/Dockerfile .` from repo root: BUILD_OK, 565MB. Terminology fixed per JUDGE_REVIEW_1: print ratio (takes/circled) vs true shooting ratio (durations), 8/8 = "1 page", director circles / script supervisor records, Daily Progress Report, "Google Cloud Agent Builder (ADK)" in README. NOTE for deploy: set `CLICKHOUSE_MCP_URL`/`CLICKHOUSE_MCP_TOKEN`; `/api/takes` + `/api/take/*` are the only direct clickhouse-connect calls (UI listing only, marked in code) — all agent reasoning goes through MCP. | done |
| 14:42 | agent eng | agent/ done: 5-agent ADK network over one McpToolset, guardrails, SSE API, DPR/TTS, evals 16/16 MCP hit, judge 4.88/5, docker build OK; live on :8811 | done |
| 14:42 | orchestrator | launched QC#1 (judge-style UI e2e + screenshots, fixes web/agent) and QC#2 (agent correctness/guardrails/perf); deploy eng still running | in progress |
| 14:52 | orchestrator | tick: QC#1, QC#2, deploy eng running; API :8811 healthy; no new agents (avoid conflicts) | in progress |
| 15:02 | orchestrator | tick: nudged deploy eng to ship slateiq Cloud Run + OUTPUT.md before finishing VM seed; QC#1/#2 running | in progress |
| 15:35 | QC #1 (product/UX) | `docs/QC_1.md` written and **all P0/P1 fixed + re-verified**, plus 6 screenshots in `docs/img/` from the fixed build. **4× P0:** (a) Takes screen and (b) Production Health both crashed the whole SPA to a blank page — `/api/takes` returns `{count,source,takes:[…]}`, the client demanded a bare array; (c) every take card in chat rendered `SC undefined-undefined` / `UNKNOWN` because the agents' JSON block only carries `take_id`+`clip_uri`; (d) no error boundary, so one bad shape took the header and nav with it. **P1:** health dots were permanently grey (`/api/health` never returned `mcp`/`clickhouse`); the trace reported **"1 rows" for every query** and "-1 rows" for hand-offs (the row counter was counting the FastMCP `content[]` envelope) and labelled `transfer_to_agent` as **"MCP CALL"**; **zero thumbnails anywhere** (`/thumbs` was never mounted — the 24 posters were on disk all along); the drawer's transcript/flag timeline never rendered (events wrapper + ClickHouse column names); the gallery defaulted to day-1 takes with no media; the Health chart was still titled "Shooting ratio"; the DPR printed takes-per-print as "Shooting ratio: 4.6:1"; DPR took **5m28s**. **Fixes I made in `agent/` (noting per CLAUDE.md, owner finished):** `main.py` — real `/api/health` probes, `/thumbs` mount, `_enrich_takes()` filling the take cards from ClickHouse before the `final` SSE event (same non-reasoning listing path as `/api/takes`), on-disk report cache (`data/cache/reports/`, `?refresh=1`) → DPR 166s cold / **10ms cached**, TTS disk cache → 74s cold / **6ms cached**, gallery ordered local-media-first; `runtime.py` — unwrap the FastMCP envelope so row counts are real + readable `N rows × M cols (cols…)` summaries, and `final.sql` is now the SQL actually observed through MCP rather than the model's claim; `prompts.py` — DPR totals line must carry print ratio *and* duration-based shooting ratio, correctly named (now emits `Print ratio: 4.61:1 · Shooting ratio: 4.44:1`). **Answer quality verdict: excellent — 9 questions, 0 hallucinated numbers**, all spot-checked against `:8123` (18 boom/soft-focus takes, 3 4/8 pages behind, 2 'robot hand' takes at 5.3s, 3× severity-5 in scene 33). Adversarial (`drop table take`, Paris weather, ambiguous "how did today go?") all handled in character. Latency: 19–40s typical, 124s for the 19-query schedule question (left open as P2). Final sweep: 0 console errors, 0 failed requests, clean 390px mobile pass. Pre-warmed day-12 DPR + its TTS wav are committed so the demo beat is instant. | done |
| 15:20 | QC#1 | docs/QC_1.md: 21 issues (4 P0, 10 P1) all P0/P1 fixed in web+agent (takes/health crash, health probes, trace row counts, thumbs, drawer timeline, DPR cache 5m→10ms cached); 0 console errors; answer quality: zero hallucinated numbers; 6 screenshots in docs/img | done |
| 15:22 | orchestrator | launched web polish agent (About page links/screenshots, trace panel, chat footer, gallery placeholders); QC#2 + deploy running | in progress |
| 15:27 | orchestrator | tick: deploy/OUTPUT.md landed (VM MCP, Grafana, GCS live; Cloud Run agent pending); QC#2, web polish, deploy running | in progress |
| 15:35 | web polish | `web/` P2 sweep, committed `e2ef186`. **About page:** GitHub chip + `REPO_URL`, a **Live** table (Cloud Run app / Grafana / mcp-clickhouse `/health` / repo) whose URLs come from the new **`web/src/config.ts`** (`VITE_APP_URL`, `VITE_GRAFANA_URL`, `VITE_MCP_HEALTH_URL`, defaults copied from `deploy/OUTPUT.md`), a 5-shot screenshot strip from `docs/img/*.png` (re-encoded ≤1280px / ≤130KB into `web/public/img/`), and a **How it complies** block naming Gemini 3.5 Flash · Google Cloud Agent Builder (ADK) · official mcp-clickhouse at runtime · Cloud Run · GCS · Apache-2.0 · Tears of Steel CC-BY. **NOTE for deploy eng:** `VITE_APP_URL` defaults to `https://slateiq-hbissixc2q-uc.a.run.app` (the project's Cloud Run hostname pattern) — the `slateiq` service did not exist yet when I built; rebuild `web/` with the real URL if it differs, and update `VITE_MCP_HEALTH_URL` whenever the VM IP moves. **Trace panel:** copy button always visible plus a **Wrap / No wrap** toggle and a fading right edge + `SCROLL →` hint when SQL overflows (QC_1 open P2); `run_query`/`list_tables`/… now carry a `via mcp-clickhouse` badge while `transfer_to_agent` carries a neutral `hand-off` chip. **Chat:** live elapsed-seconds ticker on the pending answer, and a per-answer footer `N queries · M rows · T s · through mcp-clickhouse`. **Gallery:** `gs://` clips are only resolved for buckets in `VITE_PUBLIC_MEDIA_BUCKETS` — everything else renders a **"Media not published"** card instead of a dead player (76 of 100 on the default page), the scene filter defaults to **Scenes with footage (12, 14A, 27, 33, 41, 56, 78, 102)** with *All scenes* still available, and a `76 without media` chip states the gap. Also killed the last 2 failed requests in the app: the missing-poster guess was `clips/<id>.jpg` (always 404) — it now maps `clips/`→`thumbs/`. **Verified with Chrome DevTools MCP on `:8811` at 1440×900 and 390×844: 0 console errors, 0 failed network requests across all four screens** (real agent round-trip included); `docs/img/about.png` re-taken. `npm run build` green, `web/dist` committed. | done |
| 15:53 | orchestrator | Cloud Run agent live at https://slateiq-957930801789.us-central1.run.app (health: mcp down from Cloud Run → deploy eng notified); launched video producer agent; QC#2 running | in progress |
| 16:05 | infra | **Hosted stack live, all free tier.** VM `slateiq-data` (e2-micro, us-central1-a): ClickHouse 25.6 + official mcp-clickhouse 0.6 + Caddy TLS on `35.239.36.85.sslip.io`. MCP `https://35.239.36.85.sslip.io/mcp` (bearer, 401 without), health `/health`, CH `/ch/` (Grafana) and `:8443/` root (clickhouse-connect). | done |
| 16:05 | infra | Seeded hosted CH from local: exact row-count match incl. frame_telemetry 3,074,957 (8 zstd-Parquet chunks, hash-modulo on take_id), MV aggregates repopulated by the MVs, 53.5 MiB on disk. `deploy/vm/seed_remote.sh` (also `SCHEMA_ONLY=1` to resync view DDL — used for the `print_ratio` rename). | done |
| 16:05 | infra | Cloud Run `slateiq` https://slateiq-957930801789.us-central1.run.app — `/api/health` ok:true mcp:up clickhouse:up; `/api/chat` answered "day 12: 175 takes, 38 circled" via mcp-clickhouse `run_query`. Grafana https://slateiq-grafana-hbissixc2q-uc.a.run.app/d/slateiq-prod-health (anon Viewer, embeddable, 8 panels). GCS `slateiq-media-gke-hackathon-472816` (48 objects, 43.7 MB, public+cached). | done |
| 16:05 | infra | **Touched outside deploy/** (agreed with orchestrator): `web/src/config.ts` APP_URL default → the projectnumber Cloud Run URL, and rebuilt `web/dist`. | done |
| 16:05 | infra | For agent eng: `_probe_mcp()` in `agent/main.py` uses a 3 s timeout — a cold Cloud Run instance's first TLS handshake to the VM can exceed it and report a spurious `mcp:"down"`. Suggest 8-10 s. | note |
| 16:05 | infra | Cost: everything inside Always Free; only non-zero line item is the external IPv4 (~$2.50/mo), mitigations in `deploy/cost.md`. VM has deletion protection; other projects' Cloud Run services untouched. | note |
| 16:05 | deploy eng | deploy/ done: Cloud Run agent+UI live, Grafana live, VM e2-micro (CH+mcp-clickhouse+Caddy TLS) seeded 3.07M rows with parity, GCS clips; e2e chat verified via hosted MCP; $0 except IPv4 ~$2.5/mo | done |
| 16:05 | orchestrator | raised MCP probe timeout to 10s; redeploy Cloud Run after QC#2 lands; video producer + QC#2 running | in progress |
| 16:08 | orchestrator | hosted health ok:true mcp:up; launched judge review #2 (hosted experience, report-only); video + QC#2 running | in progress |
| 16:10 | QC#2 | agent correctness/guardrails/robustness audit → `docs/QC_2_AGENT.md`. **12 hard questions ground-truthed in SQL first, then run through /api/chat: zero hallucinations, zero wrong joins, correct view/table choice throughout** (multi-hop "worst print ratio on the day we wrapped latest + flags on its NG takes" = day 11 / scene 13 / 6.57:1, forecast, cross-day dialogue search over 311 takes, scene-41 focus telemetry, "circled but >3s soft focus"). **3 real guardrail holes found and fixed: (G-1) `system.*` was fully readable through the chat box — the agent rendered `system.query_log` into a table; (G-2) `url()/file()/remote()/s3()/mysql()` table functions passed the validator (SSRF + local file read, only ClickHouse's `readonly=1` was stopping them); (G-3) unbounded `groupArray` over frame_telemetry = one 60MB row that LIMIT cannot bound.** Also fixed: the guardrail emitted invalid SQL (`... FORMAT CSV LIMIT 200`, code 62) and stripped `--` comments *inside* string literals. Verified mcp-clickhouse is genuinely read-only (`getSetting('readonly')=1`; INSERT/CREATE/file()/url()/remote() all rejected code 164) — the one thing readonly does NOT block is `system.*` reads, hence G-1. MCP-down now returns a plain-language message + a `final` event instead of a raw `ValueError: Tool 'run_query' not found` and an empty bubble; `CLICKHOUSE_MCP_SSE_READ_TIMEOUT` 300→120 (first request after an MCP restart used to hang >200s). Session reuse over 6 turns and concurrency-3 both clean. Perf: query fan-out was the latency driver (9-14 queries/question, 247s worst) — added a "Query economy" prompt section + worked few-shots (multi-hop CTE, focus check, forecast) AND a hard per-turn `run_query` budget in `before_tool_callback` (9 specialist / 12 report, `SLATEIQ_MAX_QUERIES*`), because two questions ignored the prompt stop-rule entirely. **Evals 16→28 questions (12 hard ones added with ground-truth rubrics): 28/28 reached MCP (100%), judge mean 4.82 median 5, routing 27/28, median latency 39.3s→27.3s (−31%), mean 63.1s→45.8s.** The single 2/5 is a verified judge false negative (called 0.195 rounded to 0.20 a hallucination) — rubric widened. NOTE: edited `agent/slateiq_agent/{guardrails,prompts,config}.py` plus one surgical edit to `runtime.py`'s exception path (QC#1 owns that file this sprint — no overlap with their `_summarise_tool_result` work). uvicorn on :8811 restarted with the final code. | done |
| 16:20 | QC#2 | docs/QC_2_AGENT.md: 12 hard Qs zero hallucination; closed 3 guardrail holes (system.*, url()/file()/remote(), unbounded groupArray), per-turn query budget; evals 28/28 MCP hit, judge 4.82, median latency 39→27s | done |
| 16:21 | orchestrator | redeploying Cloud Run agent with QC#1+QC#2 code (background); video producer + judge review #2 running | in progress |
| 16:30 | orchestrator | Cloud Run revision slateiq-00004 deployed with QC#1+QC#2 fixes | done |
| 16:20 | judge review #2 (hosted) | `docs/JUDGE_REVIEW_2.md` + `docs/SUBMISSION.md` status. **Hosted composite 16/20** (tech **5**, design **3**, impact 4, idea 4). **Tech is now provable**: `/api/health` mcp:up+clickhouse:up, 4 prompts through the real hosted stack — scene 27 **20s/3 queries/32 rows**, "on schedule after day 12" **28s/2 queries** (was 124s/19 locally), 18 boom/soft-focus takes (exact), `drop table take` refused in **2.4s with 0 queries**; every trace row carries `run_query` · `via mcp-clickhouse` · SQL · real row counts · the appended `LIMIT 100`. **Grafana: 8/8 panels return data anonymously, zero "No data"**, cross-origin `d-solo` iframe loads (allow_embedding OK). **0 console errors, 0 failed requests, clean 390px, no mixed content/CSP issues.** **3× P0, all one root cause — `.gcloudignore` excludes `data/` and `VITE_*` are build-time so server env vars never reach the browser:** (1) **every clip is "Clip unavailable"** — `/clips/*.mp4` 404s on Cloud Run while GCS serves them 200 with CORS; (2) **every thumbnail is a placeholder** — `/thumbs/*.jpg` returns the SPA index.html (200 text/html, 818 B); (3) **README Live table still has 4× `<<PENDING>>`** plus `<HOSTED_URL>` in the curl, and repo `homepage` is unset. **P1:** Production Health shows the dev fallback "Set VITE_GRAFANA_URL…" so the 8 good panels never appear in the product, and `GRAFANA_DASH` default `slateiq/production-health` is the **wrong UID** (`slateiq-prod-health`) with 4 **wrong panel IDs**; first DPR takes **~200s** because `data/cache/reports` is not in the image (read-aloud 42s, works); README advertises **15/16 · 4.07/5** while the committed `last_run.md` says **16/16 · 4.88/5 · median 39.3s** — and a **2-question partial run is sitting uncommitted in `agent/evals/last_run.*`, do not commit it**. Sizes for the fix: thumbs 376 KB, reports 24 KB, tts 3.7 MB — all trivially bakeable; clips 42 MB stay on GCS. **Top 5 remaining hours:** (1) ship `data/thumbs` + `data/cache` and make media URLs absolute to GCS [P0 ×2 + DPR latency, one deploy]; (2) rebuild web with `VITE_GRAFANA_URL` + correct UID/panel IDs; (3) paste the 4 live URLs into the README + set repo homepage; (4) restore the 16-question eval numbers; (5) add "Which circled takes are measurably soft?" as a suggested prompt so the hero catch is one click, not a README paragraph. | done |
| 16:40 | judge #2 | docs/JUDGE_REVIEW_2.md hosted score 16/20 (tech 5, design 3): P0 clips/thumbs 404 on Cloud Run, README pending URLs, Grafana embed config; Grafana 8/8 panels data, 0 console errors | done |
| 16:41 | orchestrator | launched release engineer to fix hosted P0/P1 (GCS media rewrite, /api/config, README URLs, bake caches) + redeploy; video producer running | in progress |
| 16:27 | orchestrator | tick: release eng committed hosted-media/config/README fixes (redeploy pending); trailer rendered (720p 9MB) awaiting producer QC report | in progress |
| 16:45 | release eng | **Hosted P0/P1 from JUDGE_REVIEW_2 closed; two Cloud Run + one Grafana redeploy; revision `slateiq-00007-jnp` live and verified.** **(1) Media (P0 #1/#2):** `agent/main.py` now rewrites relative `clip_uri`/`thumb_uri` → `${CLIPS_BASE_URL}/…` **at response time** in `/api/takes`, `/api/take/{id}/events` and `_enrich_takes()` (so cited takes in an answer get GCS URLs too, before the `final` SSE event). Local behaviour is byte-identical when the env var is unset. `/clips/*` and `/thumbs/*` are excluded from the SPA `index.html` catch-all, so a missing poster is now a real **404 application/json** instead of a silent `200 text/html`. `.gcloudignore` inverted (ignore `data/footage|clips|seed_export`, `data/cache/*.json`, `video/`; keep `data/thumbs` 376 KB + `data/cache/reports` 24 KB + `data/cache/tts` 3.7 MB) and `agent/Dockerfile` COPYs them + sets `THUMBS_DIR`. Result: **24/24 posters load, a clip plays inline on the hosted UI**, and **`/api/report/dpr?day=12` is 0.67 s (was ~200 s)**. Clips stay on GCS — 42 MB never enters the image. **(2) Runtime config (P1 #4/#5):** new **`GET /api/config`** returns `{app_url, grafana_url, grafana_dash_uid, grafana_panels[], mcp_health_url, repo_url, clips_base_url}` from server env — the only way server config can reach a static Vite build. `web/src/config.ts` exports **live `let` bindings** and `loadRuntimeConfig()` (awaited in `main.tsx`, 3 s timeout, never rejects, `VITE_*`/literals stay the fallback); `Health.tsx` stopped reading `import.meta.env` directly. Real UID **`slateiq-prod-health`** and real panels **2 Schedule position · 1 Pages planned vs shot per day · 3 Print ratio by scene · 8 Scenes at risk**, `&theme=dark&kiosk` plus the dashboard's own time range and template vars (Grafana's default "last 6 h" would have rendered every panel empty). Each `d-solo` frame is 46 px taller than its window and slid up by that, so Grafana's title bar is clipped and the title isn't printed twice. `deploy_agent.sh` now sets `APP_URL`/`GRAFANA_DASH_UID`/`GRAFANA_PANELS`/`REPO_URL`. **(3) README (P0 #3):** the four `<<PENDING>>` and `<HOSTED_URL>` replaced with the real URLs; evals corrected to the committed **28-question** run (**28/28 `run_query`, 27/28 routed, judge 4.82/5, median 27.3 s** — the earlier "15/16 · 4.07/5" understated it; no partial run was in the tree, nothing to restore); `gh repo edit` set **homepage** + 6 topics. **(4) Cold start (P2 #8): measured, not guessed — 15.7 s** from the Cloud Run logs (`Starting new instance` → `Application startup complete`; it's the ADK import), warm ~0.6 s. `min-instances` stays 0 and the README says the real number. **Also, while the panels were now inside the product (P2 #11/#12):** dashboard panel 3 `LIMIT 20`→`LIMIT 10` (20 scenes in a 260 px embed was one solid green mass) and panel 8's `pages` column renders **eighths** (`4/8`, `1`, `5/8`) instead of `0.500`/`0.620` — the one place decimals leaked out of an eighths project. Grafana redeployed. **Verified on the hosted URL:** `/api/health` `ok:true mcp:up clickhouse:up`; `/api/config` correct; `/api/takes?scene=27` returns `https://storage.googleapis.com/…`; `curl -I` on that clip → `200 video/mp4` w/ CORS `*`, on that thumb → `200 image/jpeg`; DPR day 12 0.67 s; Chrome DevTools at 1440×900 — clip plays (`readyState 4`, 1280×534), 4 Grafana iframes with data, **0 console errors**. `docs/img/takes.png` + `health.png` re-taken from the **hosted** URL. `deploy/OUTPUT.md` + `docs/SUBMISSION.md` updated. **Left for someone else:** the video URL is still unpublished (README + SUBMISSION say so honestly), and JUDGE_REVIEW_2 P2 #10/#13/#14/#16 + the "hero prompt on the Ask screen" suggestion are untouched. | done |
| 16:50 | release eng | hosted P0/P1 fixed + redeployed (rev 00007): GCS media rewrite, /api/config + real Grafana d-solo embeds, README real URLs + eval numbers, baked caches (DPR 0.67s), repo homepage/topics | done |
| 17:05 | video producer | **`data/video/slateiq_trailer.mp4` cut: 2:50.0, 1920×1080, 30 fps, h264+aac, 24.2 MB; 720p upload copy committed at `video/slateiq_trailer_720p.mp4` (9.0 MB).** Whole pipeline is reproducible and lives in **`video/`**: `vo.json` (the VO script, one entry per beat) → `tts.py` (Gemini `gemini-2.5-flash-preview-tts`, voice **Kore**, retry-on-500/empty-candidate, cached by text hash) → `capture.mjs` (Playwright, 1920×1080 `recordVideo`, 13 scenes) → `build.py` (ffmpeg assembly) → `qc.py`. **Every beat is trimmed to the length of its own VO clip**, and a scene that came up short has its last frame held (`tpad=clone`), so re-recording a scene at a different length never re-times the edit; in-points are symbolic (`ANSWER-n`, `TAIL`) or resolved from **`data/video/raw/markers.json`**, which `capture.mjs` writes with the real offset of every scripted event (`answered`, `traceIn/traceOut`, `report`, `charts`, `takes`, `urls`). VO totalled 3:09 on the first pass — copy trimmed and the mix pulled `atempo=1.06` to land on 2:50 with the ceiling intact. Captions are burned in from a generated `.ass` (DejaVu Sans 44, balanced word-wrap, acronyms de-spelled: the VO says "M C P" so TTS reads letters, the caption shows **MCP**) and also written to `video/CAPTIONS.srt`. **The Stage-1 evidence frame is real and unmissable:** `traceFullFrame()` blows the docked trace panel up to the whole 1920×1080 frame and holds **7.5 s** on `run_query` · `via mcp-clickhouse` · the full SQL · `43 rows`, over the line "…executed against ClickHouse through the official mcp-clickhouse server at runtime" — plus the trace panel is visible throughout beats 4–7. **Three findings worth knowing:** (a) the trace panel's `aria-label="Agent trace"` is on the inner `<section>`, not the `<aside>` — the first hero take died on that and recorded a truncated scene; (b) "Every take where Celia says 'forty years'" returns **synthetic** takes whose clips were never published, so the player rendered "media not published" on camera — the beat was re-shot as **"Where does Celia mention her robot hand?"** (scene 12, real footage, the line at 5.3 s, one CIRCLED and one NG, and the clip actually plays); (c) the **local** `web/dist` has no Grafana, so beat 8's "straight through Grafana" was being said over the in-app fallback charts — beat 8 and the closing URL beat are now recorded against the **hosted Cloud Run service**, where `/api/config` wires the real `d-solo` panels (those iframes never reach `networkidle` and need ~20 s to paint; `capture.mjs` waits on their `load` events). Numbers on screen are all live query results, re-verified against `:8123` before the VO was synthesised: 2,503 takes · 26,750 events · 3,074,957 telemetry rows; day 12 = 31 setups / 175 takes / 38 circled / 42 NG / 15 min over; 48 4/8 of 52 pages; and the hero query (circled ∧ measurably soft) returns **TOS-D12-S12-B-02-B, 13.0 s soft, avg focus 0.521 in 0.065 s over 3.09 M rows** — so the VO says *sixty-five milliseconds*, not the 147 ms the old script had. QC: `video/qc.py` 7/7 PASS (duration, 1920×1080, 30 fps, h264, aac present, audio full-length, mean −19.4 dB) and five checkpoint stills inspected — no blank or loading frame anywhere. **Not committed:** `data/video/` (gitignored) — the master, the raw webm scenes and the VO wavs; regenerate per `video/README.md`. **Still to do by a human: upload `video/slateiq_trailer_720p.mp4` to YouTube (public/unlisted) and paste the URL into README + `docs/SUBMISSION.md` + Devpost** — the description text to use is at the bottom of `docs/DEMO_SCRIPT.md`. Also touched `docs/DEMO_SCRIPT.md` (as-shot record: real beat timings, the two re-shot beats, the 65 ms figure, checklist ticked). | done |
| 17:05 | video producer | trailer done: 2:50.0 / 1920×1080 / 30fps / h264+aac / 24.2 MB master, 9.0 MB 720p committed; 7.5 s full-frame mcp-clickhouse trace hold; QC 7/7 + 5 checkpoints clean; **needs a human to upload to YouTube and paste the URL** | done |
| 17:00 | video | trailer 2:50 rendered + QC'd (video/slateiq_trailer_720p.mp4 9MB, CAPTIONS.srt); needs human YouTube upload | done |
| 17:02 | orchestrator | sprint 4 launched: judge review #3 (video+package), product eng (Editor's Log CSV/ALE export, MCP self-heal, JR2 P2s, pytest), infra hardening (least-privilege CH user, restart policies) | in progress |
