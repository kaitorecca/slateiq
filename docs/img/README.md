# Screenshots

The root `README.md` references these four images. Drop the PNGs in here with exactly
these filenames (1600px wide or better, dark theme, no personal data in frame):

| File | Shot |
|---|---|
| `ask.png` | **Ask the Dailies** mid-answer — streamed prose, take cards with the inline player, the agent chip showing `editor_agent`. |
| `trace.png` | The **Agent trace** panel open and readable: the `mcp-clickhouse` server label, tool name `run_query`, the generated SQL, the row count and the latency. This is the Stage-1 partner-at-runtime evidence — it must be legible at README width. |
| `takes.png` | **Takes** browser with the player drawer open on a flagged take: thumbnail grid, status badges, flag chips, transcript / flag timeline. |
| `health.png` | **Production Health** — Grafana panels plus a generated **Daily Progress Report** rendered below them. |
